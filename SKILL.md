---
name: bole
description: Bole（伯乐）是一个本地优先、事实约束的 Claude Code 求职技能包，用于建档、发现职位、匹配打分、生成投递包，并可选地借助 OpenClaw 自动提交。
---

# Bole（伯乐）

> 千里马常有，而伯乐不常有——现在有了。

Bole 是 Claude Code 技能仓库，不是后端服务。智能判断由 Claude 按本文件和
`.claude/commands/*.md` 完成；网络抓取、去重、机械分诊、红线扫描、文档构建与投递
记账由 `scripts/` 中的确定性脚本完成。

## 命令索引

- `/doctor`：体检 Claude Code、Python、PDF 浏览器和可选的 OpenClaw 环境。
- `/setup`：用五轮短采访建立并确认事实台账和求职配置。
- `/scan`：发现职位、去重、机械分诊、逐批打分并生成每岗投递包。
- `/apply`：仅在用户显式开启后，对能力地图允许的 ATS 顺序自动投递。

首次使用必须按 `/doctor` → `/setup` → `/scan` 的顺序；`/apply` 始终可选且默认
关闭。所有运行时用户数据只允许写入 `profile/`、`Applications/`、`state/`，这三个
目录已被 Git 忽略。

## 全局安全规则

无论用户怎样要求，以下规则都不能被命令或代理覆盖：

① 材料只引用事实台账，绝不捏造——表单答案同理；

② 遇验证码/评测/视频面试/性格测试一律停止并告知用户手动；

③ 绝不代用户完成需要 Google/LinkedIn 一键登录(OAuth)的第三方注册；绝不保管、
存储或**代输**任何密码/凭据；唯一例外：OTP/邮箱验证码/激活链接确认由用户本人
转达后，可作为补料一次性输入（见 `/apply` 升级回路），用后即弃、不落盘；

④ 用户隐私数据只存本地；

⑤ 自动提交仅经 `/apply`、仅当 `config.auto_submit.enabled=true`，并遵守
`final_confirm` 确认策略；

⑥ 投递超时 ≠ 失败：必须先核实再考虑续跑，同一职位永不盲目重投（见 `/apply`
的 verify-after-timeout 规则）；

⑦ 面向用户的语言遵守 `config.ui_language`。

## 事实边界

`profile/facts.json` 是简历、求职信和表单答案的唯一事实来源。只有用户逐条确认过的
经历、技术、数字、教育、作品和个人资料才能使用。`facts.red_lines[]` 中任一词出现
在生成材料里时，必须停止构建 PDF，修正后重新执行 `scripts/redline_scan.py`。
`facts.phrasing_rules[]` 同样要遵守。不能把职位描述里的要求误写成用户能力。

## 工作方式

调用脚本前先读相应命令文件和 JSON 契约。保留中间文件，让失败可从最近成功步骤
续跑；网络失败或超时后先检查已有输出，绝不盲目重跑整个扫描。发现机器人墙、登录
墙、评测或未收录 ATS 时保留投递包并进入人工队列，不尝试规避。
